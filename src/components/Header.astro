---
import type { Profile } from '../data/site';

interface Props {
  profile: Profile;
}

const { profile } = Astro.props;
---

<header class="header">
  <div class="header-content" data-depth-reveal>
    <h1 class="name">{profile.name}</h1>
    <div class="bio">{profile.bio}</div>

    <div class="links">
      {
        profile.social.map((item) => (
          <a href={item.url} target="_blank" rel="noopener noreferrer" class="link-button">
            <i class={`ph ${item.icon}`} />
            {item.label}
          </a>
        ))
      }
      {
        profile.resume.visible && (
          <a
            href={profile.resume.url}
            target="_blank"
            rel="noopener noreferrer"
            class="link-button"
          >
            <i class="ph ph-file-text" />
            Resume
          </a>
        )
      }
    </div>
  </div>

  <div class="header-avatar-frame" data-depth-reveal style="--reveal-delay: 120ms">
    <img
      class="header-avatar"
      data-parallax-depth="16"
      alt={`${profile.name} avatar`}
      src={profile.avatar}
    />
  </div>
</header>
